name: Python

on:
  push:
    branches: [ main ]
    paths:
      - 'noisemaker/**'
      - 'share/**'
      - 'pyproject.toml'
      - 'requirements.txt'
      - 'tox.ini'
      - '.github/workflows/python.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'noisemaker/**'
      - 'share/**'
      - 'pyproject.toml'
      - 'requirements.txt'
      - 'tox.ini'
      - '.github/workflows/python.yml'

jobs:
  test:
    name: Python tests
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run pytest
      run: pytest -v

  lint:
    name: Python lint
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run black
      run: black --check noisemaker

    - name: Run ruff
      run: ruff check noisemaker

  type-check:
    name: Python type-check
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run mypy
      run: mypy noisemaker --ignore-missing-imports
