# Pixels

Pixelation effect. Reduces image resolution for a retro pixel art look.

## Parameters

| Parameter | Type | Default | Range | Description |
|-----------|------|---------|-------|-------------|
| size | float | 4.0 | 1–32 | Pixel block size |

## Usage

```dsl
read().pixels(size: 8).write(o0)
```
