# OKLab

Treats RGB channels as OKLab values and converts to RGB. Useful for perceptually uniform color manipulation.

## Parameters

This effect has no parameters.

## Usage

```dsl
read().srgb2oklab().write(o0)
```
